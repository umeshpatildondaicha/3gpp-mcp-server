apiVersion: v1
kind: Service
metadata:
  name: 3gpp-mcp-server
  labels:
    app: 3gpp-mcp-server
spec:
  selector:
    app: 3gpp-mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
