server:
  port: ${PORT:3000}

spring:
  ai:
    mcp:
      server:
        protocol: STREAMABLE
        name: 3gpp-telecom-kb
        version: 2.0.0
        type: SYNC
        instructions: "3GPP Telecom knowledge-base MCP server"
        streamable-http:
          mcp-endpoint: /mcp

app:
  session-ttl-ms: ${SESSION_TTL_MS:1800000}
  kb-db-path: ${KB_DB_PATH:}
