<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.vwaves</groupId>
    <artifactId>3gpp-mcp-server</artifactId>
    <version>2.0.0</version>
    <name>3gpp-mcp-server</name>
    <description>3GPP Telecom KB HTTP/SSE MCP Server (Spring Boot, Java 21)</description>

    <properties>
        <java.version>21</java.version>
        <spring.boot.version>3.3.5</spring.boot.version>
        <spring.ai.version>1.1.4</spring.ai.version>
        <!-- Knowledge-base source directory; override with -Dkb.db.dir=... -->
        <kb.db.dir>${project.basedir}/3gpp_kb</kb.db.dir>

        <!-- SonarQube analysis. Host URL and token come from the CLI / CI env:
             mvn verify sonar:sonar -Dsonar.host.url=... -Dsonar.token=... -->
        <sonar.projectKey>com.vwaves:3gpp-mcp-server</sonar.projectKey>
        <sonar.projectName>3gpp-mcp-server</sonar.projectName>
        <sonar.java.source>${java.version}</sonar.java.source>
        <sonar.sourceEncoding>UTF-8</sonar.sourceEncoding>
        <sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
        <sonar.exclusions>**/target/**,**/3gpp_kb/**,**/*.db,ui/**,bench/**,pg/**</sonar.exclusions>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring.ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-transformers</artifactId>
        </dependency>
        <!--
          The transformers starter drags in ai.djl.pytorch:pytorch-engine, and DJL
          initialises that engine at startup even though this app only uses ONNX
          Runtime + the HuggingFace tokenizer. Without the two dependencies below,
          DJL downloads ~105 MB of libtorch natives from publish.djl.ai on EVERY
          container start (the cache dir dies with the container) — in the railtel
          cluster that download blew the liveness-probe budget and crash-looped the
          pod 227 times. Bundling the linux-x86_64 natives in the jar makes startup
          fully offline. Versions are paired: pytorch-jni is <libtorch>-<djl>, and
          the DJL version must match what spring-ai-transformers imports (0.32.0).
        -->
        <dependency>
            <groupId>ai.djl.pytorch</groupId>
            <artifactId>pytorch-jni</artifactId>
            <version>2.5.1-0.32.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>ai.djl.pytorch</groupId>
            <artifactId>pytorch-native-cpu</artifactId>
            <classifier>linux-x86_64</classifier>
            <version>2.5.1</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.46.1.3</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <excludes>
                    <exclude>*.db</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${java.version}</release>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>copy-runtime-bundle</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/docker</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/src/main/resources</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>copy-jar-to-docker-folder</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.build.directory}/docker"/>
                                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                      tofile="${project.build.directory}/docker/${project.build.finalName}.jar"/>

                                <!--
                                  Stage the SQLite knowledge bases next to the JAR when present.
                                  When 3gpp_kb/*.db sits beside pom.xml (typical local-dev / build-machine
                                  layout), they are copied into target/docker/ so the subsequent
                                  `tar` + `docker build` produces a fully self-contained image.
                                  If the DBs are absent (e.g. CI without the 3.4 GB seed data) the
                                  build silently skips them and the resulting image expects KB_DB_PATH
                                  / KB_DB_PATH2 to point at a mounted volume at runtime.
                                  Override the source dir with -Dkb.db.dir=/path/to/3gpp_kb.
                                -->
                                <mkdir dir="${project.build.directory}/docker/3gpp_kb"/>
                                <copy todir="${project.build.directory}/docker/3gpp_kb" failonerror="false" quiet="true">
                                    <fileset dir="${kb.db.dir}" erroronmissingdir="false">
                                        <include name="3gpp_certified.db"/>
                                        <include name="telecom_extras.db"/>
                                    </fileset>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>5.7.0.6970</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- Byte Buddy (via Mockito) needs the experimental flag on JDKs newer
                         than it officially supports (local toolchain is JDK 26). -->
                    <argLine>@{argLine} -Dnet.bytebuddy.experimental=true</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.13</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Activated by the VisionWaves Jenkins pipeline via -Pvisionwaves -->
        <profile>
            <id>visionwaves</id>
        </profile>
    </profiles>
</project>
